# Normalise line endings in the repository. Contributors on Linux and on Windows
# must produce identical blobs, otherwise every checkout looks like a rewrite.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.mp3 binary
*.kb binary
*.kbld binary
