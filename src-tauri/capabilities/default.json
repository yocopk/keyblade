{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Deliberately minimal: every entry added here widens what a compromised web layer can reach, so the list stays as short as the application can function with. core:path:default is here only so the music file can be located; the asset protocol itself is not a capability permission, it is restricted by assetProtocol.scope in tauri.conf.json.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:path:default"
  ]
}
